# FB 直播自動留言系統使用說明書

本說明適用於 Windows 版 `fb-live-comment-extension/dist/FB_Live_Auto_Comment.exe`。系統支援多個 Facebook 帳號參與同一場直播，也支援一個帳號分別處理多場直播。每列直播可獨立開始、暫停、繼續與停止。

## 一、安裝與首次設定

請保留整個 `final_project` 資料夾，讓主程式、LLM、STT 和 Chrome 擴充功能維持原本的相對位置。

1. 執行 `fb-live-comment-extension/dist/FB_Live_Environment_Setup.exe`。它會檢查 Chrome、MongoDB、Ollama，並下載 `nomic-embed-text` 模型；安裝軟體時可能要求系統管理員權限。
2. 自動模式使用的模型是 `qwen3:8b`。環境安裝程式目前不會下載它，請在 PowerShell 執行：

   ```powershell
   ollama pull qwen3:8b
   ```

3. 在 Chrome 開啟 `chrome://extensions`，開啟「開發人員模式」，按「載入未封裝項目」，選取 `final_project/fb-live-comment-extension/chrome_extension` 資料夾。確認 **FB Live Auto Clicker** 已啟用。
4. 每個機器人帳號使用的 Chrome 設定檔，都要安裝並啟用擴充功能。更新擴充功能檔案後，請在各設定檔的 `chrome://extensions` 按「重新載入」，並重新開啟直播頁面。
5. 確認 MongoDB 和 Ollama 正在執行，再開啟 `fb-live-comment-extension/dist/FB_Live_Auto_Comment.exe`。

若系統找不到 `nomic-embed-text`，可執行 `ollama pull nomic-embed-text`。

## 二、設定機器人帳號

左上方「機器人管理」最多可顯示 **8 個帳號**。按「新增帳號」後，程式會用獨立的 Chrome 設定檔開啟 Facebook；請在該視窗登入要使用的帳號。A 對應 Chrome `Default`、B 對應 `Profile 1`、C 對應 `Profile 2`，依序類推。登入資料保存在各自的 Chrome 設定檔。

勾選機器人字卡，可指定哪些帳號參與接下來啟動的直播列。可同時勾選多個帳號。字卡顯示名稱可自行編輯；它只是介面名稱，不會更換 Facebook 登入帳號。

按字卡上的關閉按鈕會移除字卡；若該帳號正在使用中，相關直播列也會停止。Chrome 設定檔與登入資料仍保留，再按「新增帳號」可重新顯示。

## 三、加入與操作直播網址

「直播來源管理」最多可加入 **5 列** Facebook 直播網址。每列可單獨操作：

1. 勾選要使用的機器人帳號。
2. 在直播列輸入 Facebook 直播頁面的完整網址，例如 `https://www.facebook.com/.../videos/...`。
3. 按該列的「▶ 開始」。程式會啟動該列的語音轉文字（STT）、對應帳號的 LLM 終端機，並以各帳號的 Chrome 設定檔開啟直播頁。
4. 執行中的同一按鈕可「暫停」；暫停後按「繼續」即可接續。
5. 按「■ 停止」會停止該列流程、關閉該列對應的 LLM 終端機，並將原本的網址恢復成可編輯狀態。其他執行中的直播列不受影響。

**一對多**：選取一個帳號，在多列輸入不同直播網址並逐列開始。每列使用獨立 STT 資料與 LLM 終端機，不會把不同直播的語音內容混在一起。

**多對一**：同時勾選多個帳號，在同一列輸入一個直播網址並開始。該列共用一次 STT 擷取，每個帳號各有自己的 LLM 終端機與 Chrome 視窗。多個帳號參與多列直播時，會依「帳號 × 直播列」分開處理。

## 四、設定留言語句與回覆模式

右側「管理語句 · 直播 N」可切換目前正在編輯的直播列。請先選對直播列，再調整該列設定。

### 語句列表

- **預設／自訂／衝人氣**：這三個按鈕只切換語句分類，不是回覆模式。按下後立即顯示對應列表，不需要按「套用」。衝人氣語句保存在獨立資料庫。
- **勾選**：三類語句都能啟用或停用。切換列表分類不會清除其他分類的設定；手動與自動模式都會使用目前已啟用的三類語句。
- **新增與編輯**：在「新增留言語句」輸入文字後按「＋ 新增語句」。自訂語句可用鉛筆圖示修改，再按「儲存修改」。選取要刪的自訂語句後按「刪除選取」。預設語句可停用，但不能修改或刪除。
- **衝人氣語句**：切到「衝人氣」分類後可查看、勾選、新增、編輯與刪除口號。

### 回覆模式

| 模式 | 行為 |
| --- | --- |
| 手動 | 對直播語音和已啟用的預設、自訂、衝人氣語句做向量比對；只會逐字送出資料庫現有語句。最近 5 次已送出的普通留言不會重複，衝人氣語句不受此限制。 |
| 自動 | 把已啟用的預設、自訂、衝人氣語句連同直播語音交給 `qwen3:8b`；模型可採用現有語句，也能生成資料庫以外的新留言。若連續三次未產生回覆，系統可送出一次簡短的固定備援語句。 |

按「手動」或「自動」只是選取模式，**必須再按「✓ 套用」才會生效**。按鈕在回覆模式或語句勾選狀態變更時會亮起；單純切換語句分類不會讓它亮起。勾選狀態會立即儲存，「套用」也會重新整理目前直播列的設定。

自動模式需要累積足夠的語音文字才會嘗試生成回覆，因此開始直播後不一定立即留言。手動模式會直接依每次收到的主播文字做向量比對；沒有達到相似度門檻的現有語句時不留言。

## 五、常用直播主

在直播列輸入網址後，程式會嘗試透過 STT 辨識直播主名稱與 Facebook 個人主頁。成功時會顯示在左下角「常用直播主」，最多保留 **3 位**，最近輸入的排在最上方。儲存的是直播主個人主頁網址，而非該場直播網址。

按「前往」會以目前選取帳號的 Chrome 設定檔開啟該個人主頁；按「刪除」會從常用清單移除。若直播資料無法辨識或找不到個人主頁，就不會加入清單。

## 六、常見問題

- **Chrome 有開啟，但沒有回覆**：確認該 Chrome 設定檔已安裝並啟用 **FB Live Auto Clicker**、該列已開始且回覆模式已按「套用」。若使用手動模式，還要確認至少有一筆已啟用語句達到向量相似度門檻。
- **自動模式沒有立刻留言**：先確認直播有可辨識的語音；系統會等文字累積並判斷回覆時機。
- **模型找不到或 LLM 啟動失敗**：確認 Ollama 正在執行，並執行 `ollama pull qwen3:8b` 和 `ollama pull nomic-embed-text`。
- **MongoDB 或語句列表無法讀取**：確認 MongoDB 服務正常，再重新開啟主程式。
- **某帳號沒有留言**：確認該帳號字卡已勾選、它的 Chrome 設定檔已登入 Facebook，且擴充功能已在該設定檔啟用。再查看標有帳號與直播列的 LLM 終端機訊息。
- **停止後仍顯示直播主名稱**：按該列「停止」後，網址欄應恢復原輸入網址並可修改；若仍在啟動中，等停止作業完成後再檢查。
- **常用直播主沒有新增**：輸入有效的 Facebook 直播網址，等待 STT 辨識；若無法取得個人主頁網址，清單不會新增。

開發與檔案配置請見 [README.md](README.md)。
