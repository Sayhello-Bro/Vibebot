# Used ONLY by Docker Compose; Python and run_json_test.command do not load .env.
# Copy to .env, enter your own local password, and never commit .env.
# For passwords containing $ or #, use Compose single-quoted syntax.
# Example syntax only: MONGO_PASSWORD='your-own-password'
MONGO_PASSWORD=
